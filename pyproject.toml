[project]
name = "pbs"
version = "0.1.0"
description = "FastAPI backend with a Bun, React, Tailwind, and shadcn frontend"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn>=0.52.4",
]
